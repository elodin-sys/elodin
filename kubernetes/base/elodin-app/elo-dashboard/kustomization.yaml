apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: elo-dashboard
    includeSelectors: true

resources:
  - deployment.yaml
  # - autoscaler.yaml
  - service.yaml
