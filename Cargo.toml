[package]
name = "paracosm"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["rerun"]
rerun = ["dep:rerun"]

[dependencies]
nalgebra = "0.32"
stable-vec = "0.4"

rerun.version = "0.7.0"
rerun.optional = true

[dev-dependencies]
plotters = "0.3.5"
approx = "0.5"
