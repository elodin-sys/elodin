# logical_key

Logical keyboard state tracking across frames.

## What it does
- Records logical keys in `pressed`, `just_pressed`, `just_released` sets.
- Updates state from `KeyboardInput` events in `PreUpdate`.

## Main API
- `LogicalKeyPlugin`
- `LogicalKeyState`

## Status
Active.
