These fixtures are tiny AAC-in-ADTS frames used by tests.

Each `.adts` file contains a single ADTS frame encoded as a hex string.
The payload bytes are dummy data; tests only require structurally valid ADTS framing.
